# Telemt tools

### Description
API for handling Telemt proxy clients.\
Fast API and Telemt are running in docker containers.

### API Specification
see [spec.yml](server/api/spec.yaml)

### Deploy
```bash
docker compose up --build -d
```